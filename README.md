# slop-mop.com

Website for the slop-mop quality gate orchestration tool.

## Local development

```bash
# Serve locally
npx http-server
```

## Structure

- `index.html` — landing page
- `docs/` — documentation (synced from slop-mop repo)
- `assets/` — images, styles, etc.
